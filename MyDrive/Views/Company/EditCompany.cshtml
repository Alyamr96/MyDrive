@model MyDrive.Models.Company
@{
    ViewBag.Title = "CreateCompany";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create Company</h2>

@using (Html.BeginForm("EditCompany1", "Company", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="form-group">
        <label>New Company Name</label>
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Name)
    </div>

    <div class="form-group">
        <label>Choose File which is png or jpg or jpeg or jfif:</label>
        <input type="file" id="userFile" name="userFile" /><br />
    </div>
    @Html.HiddenFor(m => m.Id)
    <button type="submit" class="btn btn-primary">Save Company</button>

} 